#include "main.h"
#include <stdio.h>
/**
 *more_numbers - prints the numbers, from 0 to 9
 *Return: product.
 */
void more_numbers(void)
{
int a;
int b;
for (b = 0; b <= 9; b++)
{
for (a = 0; a <= 14; a++)
{
printf("%d", a);
}
printf("\n");
}
}
