#include <stdio.h>
/**
 * _isupper - the main function
 * if_else - my loop function
 * Return: Description of the returned value
 */
int _isupper(int c) {
if ( c >= 'A' && c <= 'Z' )
{
return (1);
}
else
{
return (0);
}
}
